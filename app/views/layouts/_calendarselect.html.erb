 <% @user = User.find(current_user.id) %>
    <%= simple_form_for(@user, :html=> { class: '' }) do |f| %>
        <%= f.association :calendar, :input_html => {:onchange => '$(this.form).submit();', :class => 'browser-default'}, :label => false, label_method: :name, value_method: :id, prompt: t('select') %>
    <% end %>  